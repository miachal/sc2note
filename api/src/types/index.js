const campaign = require('./CampaignType');
const career = require('./CareerType');
const player = require('./PlayerType');
const snapshot = require('./SnapshotType');
const swarm = require('./SwarmType');
const root = require('./root');

module.exports = [
  campaign,
  career,
  player,
  snapshot,
  swarm,
  root
];